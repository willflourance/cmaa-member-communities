/*!
 * Start Bootstrap - Creative v4.0.0-beta.2 (https://startbootstrap.com/template-overviews/creative)
 * Copyright 2013-2017 Start Bootstrap
 * Licensed under MIT (https://github.com/BlackrockDigital/startbootstrap-creative/blob/master/LICENSE)
 */
! function (a) {
	"use strict";
	a('a.js-scroll-trigger[href*="#"]:not([href="#"])').click(function () {
		if (location.pathname.replace(/^\//, "") == this.pathname.replace(/^\//, "") && location.hostname == this.hostname) {
			var e = a(this.hash);
			if ((e = e.length ? e : a("[name=" + this.hash.slice(1) + "]")).length) return a("html, body").animate({
				scrollTop: e.offset().top - 57
			}, 1e3, "easeInOutExpo"), !1
		}
	}), a(".js-scroll-trigger").click(function () {
		a(".navbar-collapse").collapse("hide")
	}), a("body").scrollspy({
		target: "#mainNav",
		offset: 57
	});
	
	
/*	var e = function () {
		a("#mainNav").offset().top > 100 ? a("#mainNav").addClass("navbar-shrink") : a("#mainNav").removeClass("navbar-shrink")
	};*/
	
	
/* if window scrolls from top more than 100px - full color 
	$(window).scroll(function() {
    if ($(".navbar").offset().top > 100) {
        $('#custom-nav').addClass('affix');
        $(".navbar-fixed-top").addClass(".navbar-collapse");
        $('.navbar-brand img').attr('src','img/logos/ClubFoundation_Logo4C_Horiz.png');
		$("#mainNav").addClass("navbar-shrink");
		//change src
    } 
});
		*/


	
	
	
	
/* if window is less than 992px - full color 
	
if($( window ).width() < 992) { 
        $(".navbar-brand img").attr("src","img/logos/ClubFoundation_Logo4C_Horiz.png");
			$("#mainNav").addClass("navbar-shrink");

}*/
	
/*if window is resized to less than 992px -full color
	
	$( window ).resize(function() {
    if($( window ).width() < 992) { 
        $(".navbar-brand img").attr("src","img/logos/ClubFoundation_Logo4C_Horiz.png");
				$("#mainNav").addClass("navbar-shrink");

		
    } 
}); */	

/* if window is resized to more than 992px - White 
	
		$( window ).resize(function() {
    if($( window ).width() > 992) { 
        $(".navbar-brand img").attr("src","img/logos/ClubFoundation_LogoBW_Horiz.png");
		$("#mainNav").removeClass("navbar-shrink");
		
    } 
});
	
	/* if window is  more than 992px - White 
	if($( window ).width() > 992) { 
        $(".navbar-brand img").attr("src","img/logos/ClubFoundation_LogoBW_Horiz.png");
				$("#mainNav").removeClass("navbar-shrink");
	

}*/
	
	
	e(), a(window).scroll(e), window.sr = ScrollReveal(), sr.reveal(".sr-icons", {
		duration: 600,
		scale: .3,
		distance: "0px"
	}, 200), sr.reveal(".sr-button", {
		duration: 1e3,
		delay: 200
	}), sr.reveal(".sr-contact", {
		duration: 600,
		scale: .3,
		distance: "0px"
	}, 300), a(".popup-gallery").magnificPopup({
		delegate: "a",
		type: "image",
		tLoading: "Loading image #%curr%...",
		mainClass: "mfp-img-mobile",
		gallery: {
			enabled: !0,
			navigateByImgClick: !0,
			preload: [0, 1]
		},
		image: {
			tError: '<a href="%url%">The image #%curr%</a> could not be loaded.'
		}
	})
}(jQuery);





filterSelection("all")
function filterSelection(c) {
  var x, i;
  x = document.getElementsByClassName("filterDiv");
  if (c == "all") c = "";
  // Add the "show" class (display:block) to the filtered elements, and remove the "show" class from the elements that are not selected
  for (i = 0; i < x.length; i++) {
    w3RemoveClass(x[i], "show");
    if (x[i].className.indexOf(c) > -1) w3AddClass(x[i], "show");
  }
}

// Show filtered elements
function w3AddClass(element, name) {
  var i, arr1, arr2;
  arr1 = element.className.split(" ");
  arr2 = name.split(" ");
  for (i = 0; i < arr2.length; i++) {
    if (arr1.indexOf(arr2[i]) == -1) {
      element.className += " " + arr2[i];
    }
  }
}

// Hide elements that are not selected
function w3RemoveClass(element, name) {
  var i, arr1, arr2;
  arr1 = element.className.split(" ");
  arr2 = name.split(" ");
  for (i = 0; i < arr2.length; i++) {
    while (arr1.indexOf(arr2[i]) > -1) {
      arr1.splice(arr1.indexOf(arr2[i]), 1); 
    }
  }
  element.className = arr1.join(" ");
}

// Add active class to the current control button (highlight it)
var btnContainer = document.getElementById("myBtnContainer");
var btns = btnContainer.getElementsByClassName("btn");
for (var i = 0; i < btns.length; i++) {
  btns[i].addEventListener("click", function() {
    var current = document.getElementsByClassName("active");
    current[0].className = current[0].className.replace(" active", "");
    this.className += " active";
  });
}
